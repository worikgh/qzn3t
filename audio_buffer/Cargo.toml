[package]
name = "qzn3t_audio_buffer"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dependencies.uuid]
version = "1.21"
# Lets you generate random UUIDs
features = [
    "v6",
]
